import { Text } from "@chakra-ui/react";
import FlexCol from "@/components/layout/FlexCol";

const Mv = () => {
  return (
    <FlexCol>
      <Text>MV</Text>
    </FlexCol>
  );
};

export default Mv;
